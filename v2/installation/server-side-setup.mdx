---
title: Server-Side Setup
---

import V2UpgradeWarning from "/snippets/v2-upgrade-warning.mdx";

<V2UpgradeWarning />

The first step when installing Inertia is to configure your server-side framework. These docs are written from the perspective of **InertiaCore**, the ASP.NET Core adapter — `AspNetCore.InertiaCore` on NuGet. Examples for the [Laravel adapter](https://inertiajs.com) are included as a tab in every code block so you can follow along in either runtime. For other frameworks, see the [community adapters](/v2/installation/community-adapters).

<Card
    icon="dotnet"
    title="Scaffold a new InertiaCore app"
    href="https://www.npmjs.com/package/create-inertiacore"
    arrow="true"
    cta="Start building"
>
The fastest way to start a new Inertia project on ASP.NET Core is the `create-inertiacore` scaffolder, which wires up `AspNetCore.InertiaCore`, Vite, and your frontend framework of choice (Vue, React, or Svelte) in one step:

```sh
npm init inertiacore@latest
# or
npx create-inertiacore@latest
```

If you'd prefer to add InertiaCore to an existing ASP.NET Core project, follow the manual steps below.

</Card>

## Installation

<Steps>
    <Step title="Install dependencies">
        Install the InertiaCore server-side adapter. The Vite helper is optional but recommended — it auto-wires `Inertia.Version(Vite.GetManifestHash)` when your assets change.

        <CodeGroup>

        ```sh .NET icon="dotnet"
        dotnet add package AspNetCore.InertiaCore
        ```

        ```bash Laravel icon="laravel"
        composer require inertiajs/inertia-laravel
        ```

        </CodeGroup>
    </Step>

    <Step title="Setup root template">
        Next, create the root view that will be loaded on the first page visit. InertiaCore renders it via `@await Inertia.Html(Model)` (and optionally `@await Inertia.Head(Model)` for SSR). By default the root view lives at `~/Views/App.cshtml`; override it through `AddInertia(options => options.RootView = ...)` if needed.

        <CodeGroup>

        ```cshtml .NET icon="dotnet"
        @using InertiaCore
        <!DOCTYPE html>
        <html>
            <head>
                <meta charset="utf-8" />
                <meta name="viewport" content="width=device-width, initial-scale=1">
                @Vite.Input("src/app.tsx")
                @await Inertia.Head(Model)
            </head>
            <body>
                @await Inertia.Html(Model)
            </body>
        </html>
        ```

        ```blade Laravel icon="laravel"
        <html>
            <head>
                <meta charset="utf-8" />
                <meta name="viewport" content="width=device-width, initial-scale=1">
                @vite('resources/js/app.js')
                @inertiaHead
            </head>
            <body>
                @inertia
            </body>
        </html>
        ```

        </CodeGroup>

        For React applications, it's recommended to include the `@viteReactRefresh` directive (Laravel) or the React refresh runtime (InertiaCore) before your main entry script to enable Fast Refresh in development.

        `Inertia.Html(Model)` renders a `<div>` with an `id` of `app` as the mounting point for your JavaScript application. You may customize the `id` by passing a second argument.

        <CodeGroup>

        ```cshtml .NET icon="dotnet"
        @using InertiaCore
        <!DOCTYPE html>
        <html>
            ...
            <body>
                @await Inertia.Html(Model, "custom-app-id")
            </body>
        </html>
        ```

        ```blade Laravel icon="laravel"
        <html>
            ...
            <body>
                @inertia('custom-app-id')
            </body>
        </html>
        ```

        </CodeGroup>

        If you change the `id` of the root element, be sure to update it [client-side](/v2/installation/client-side-setup#defining-a-root-element) as well.
    </Step>

    <Step title="Register InertiaCore">
        Register InertiaCore in `Program.cs` with `AddInertia()` and add the middleware with `UseInertia()`.

        <CodeGroup>

        ```csharp .NET icon="dotnet"
        // Program.cs
        using InertiaCore.Extensions;

        var builder = WebApplication.CreateBuilder(args);

        builder.Services.AddControllersWithViews();
        builder.Services.AddInertia();
        builder.Services.AddViteHelper();

        var app = builder.Build();

        app.UseStaticFiles();
        app.UseInertia();
        app.MapControllers();

        app.Run();
        ```

        ```php Laravel icon="laravel"
        // bootstrap/app.php
        use App\Http\Middleware\HandleInertiaRequests;

        ->withMiddleware(function (Middleware $middleware) {
            $middleware->web(append: [
                HandleInertiaRequests::class,
            ]);
        })
        ```

        </CodeGroup>

        `UseInertia()` registers the InertiaCore middleware, which handles protocol headers, partial reloads, validation-error forwarding, and asset versioning. Use `Inertia.Version(...)` to set your [asset version](/v2/advanced/asset-versioning) and `Inertia.Share(...)` to define [shared data](/v2/data-props/shared-data).
    </Step>

    <Step title="Create your first response">
        That's it, you're all ready to go server-side! Return an Inertia response from a controller with `Inertia.Render("Component", props)`.

        <CodeGroup>

        ```csharp .NET icon="dotnet"
        using InertiaCore;

        public class EventsController : Controller
        {
            private readonly AppDbContext _context;

            public EventsController(AppDbContext context) => _context = context;

            [HttpGet("/events/{id:int}")]
            public IActionResult Show(int id)
            {
                var evt = _context.Events.Find(id);

                return Inertia.Render("Event/Show", new
                {
                    Event = new
                    {
                        evt.Id,
                        evt.Title,
                        evt.StartDate,
                        evt.Description,
                    },
                });
            }
        }
        ```

        ```php Laravel icon="laravel"
        use Inertia\Inertia;

        class EventsController extends Controller
        {
            public function show(Event $event)
            {
                return Inertia::render('Event/Show', [
                    'event' => $event->only(
                        'id',
                        'title',
                        'start_date',
                        'description',
                    ),
                ]);
            }
        }
        ```

        </CodeGroup>
    </Step>
</Steps>
